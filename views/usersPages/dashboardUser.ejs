<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>نظام إدارة الإجازات المرضية - لوحة التحكم</title>
    
    <!-- Bootstrap 5.3 RTL -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

    <link rel="stylesheet" href="css/main/main.css">
    <link rel="stylesheet" href="css/main/modern-styles.css">
</head>
<body>
    <!-- Header -->
    <%- include ('./nav/navUser') %>
   <!-- Main Content -->
   <div class="main-content">
    <div class="container-fluid">
        <!-- Welcome Section with Real-time Info -->
        <div class="welcome-section fade-in">
            <div class="welcome-header">
                <h2>
                    <i class="fas fa-tachometer-alt me-2"></i>
                    لوحة التحكم الرئيسية
                </h2>
                <p class="mb-0">نظام إدارة الإجازات المرضية المتطور - متابعة شاملة وإدارة فعالة</p>
            </div>
            
            <div class="welcome-content">
                <div class="welcome-message">
                    <h3 class="greeting"></h3>
                    <p class="greeting" id="timeGreeting">نتمنى لك يوماً سعيداً</p>
                </div>
                
                <div class="time-info">
                    <div class="current-time" id="currentTime">--:--:--</div>
                    <div class="date-info">
                        <div class="date-card">
                            <div class="label">التاريخ الهجري</div>
                            <div class="value" id="hijriDate">-- --- ----</div>
                        </div>
                        <div class="date-card">
                            <div class="label">التاريخ الميلادي</div>
                            <div class="value" id="gregorianDate">-- --- ----</div>
                        </div>
                    </div>
                </div>
                

            </div>
        </div>

        <!-- Real-time Statistics Cards -->
        <div class="stats-grid slide-up">
            <div class="stat-card">
                <div class="number" id="activeUsers"></div>
                <div class="title">مستخدم نشط</div>
                <div class="change positive">
                </div>
            </div>
            <div class="stat-card">
                <div class="number" id="monthlyLeaves"></div>
                <div class="title">إجازة هذا الشهر</div>
                <div class="change positive">
                </div>
            </div>
            <div class="stat-card">
                <div class="number" id="todayLeaves"></div>
                <div class="title">إجازة اليوم</div>
                <div class="change negative">
                </div>
            </div>

        </div>

        <!-- Quick Actions Section Header -->
        <div class="section-header">
            <div class="card-header">
                <i class="fas fa-bolt"></i>
                الإجراءات السريعة
            </div>
        </div>

        <!-- Compact Quick Actions Cards -->
        <div class="compact-action-cards">
            <div class="compact-action-card">
                <div class="compact-card-header">
                    <div class="compact-card-icon">
                        <i class="fas fa-plus-circle"></i>
                    </div>
                    <div class="compact-card-title">
                        <h6>إضافة إجازة جديدة</h6>
                    </div>
                </div>
                <div class="compact-card-content">
                    <p class="compact-card-description">إنشاء طلب إجازة مرضية جديد وإدخال بيانات المريض</p>
                    <div class="compact-card-action">
                        <a href="/add_leaveUser" class="compact-btn">
                            <i class="fas fa-plus"></i>
                            إضافة الآن
                        </a>
                    </div>
                </div>
            </div>

            <div class="compact-action-card">
                <div class="compact-card-header">
                    <div class="compact-card-icon">
                        <i class="fas fa-check-circle"></i>
                    </div>
                    <div class="compact-card-title">
                        <h6>إدارة الإجازات المرضية</h6>
                    </div>
                </div>
                <div class="compact-card-content">
                    <p class="compact-card-description">عرض ومراجعة وطباعة تقارير الإجازات المرضية</p>
                    <div class="compact-card-action">
                        <a href="/manage_leavesUser" class="compact-btn">
                            <i class="fas fa-list"></i>
                            عرض التقارير
                        </a>
                    </div>
                </div>
            </div>

            <!-- <div class="compact-action-card">
                <div class="compact-card-header">
                    <div class="compact-card-icon">
                        <i class="fas fa-users"></i>
                    </div>
                    <div class="compact-card-title">
                        <h6>إدارة المستخدمين</h6>
                    </div>
                </div>
                <div class="compact-card-content">
                    <p class="compact-card-description">إضافة وتعديل وحذف المستخدمين وتعيين الصلاحيات</p>
                    <div class="compact-card-action">
                        <a href="/manage_users_modern" class="compact-btn">
                            <i class="fas fa-user-cog"></i>
                            إدارة المستخدمين
                        </a>
                    </div>
                </div>
            </div> -->

            <div class="compact-action-card">
                <div class="compact-card-header">
                    <div class="compact-card-icon">
                        <i class="fas fa-search"></i>
                    </div>
                    <div class="compact-card-title">
                        <h6>الاستعلام عن الإجازات</h6>
                    </div>
                </div>
                <div class="compact-card-content">
                    <p class="compact-card-description">البحث والاستعلام عن الإجازات المرضية المسجلة</p>
                    <div class="compact-card-action">
                        <a href="/quary" class="compact-btn">
                            <i class="fas fa-search"></i>
                            الاستعلام الآن
                        </a>
                    </div>
                </div>
            </div>

            <!-- <div class="compact-action-card">
                <div class="compact-card-header">
                    <div class="compact-card-icon">
                        <i class="fas fa-file-alt"></i>
                    </div>
                    <div class="compact-card-title">
                        <h6>بطاقات التشغيل</h6>
                    </div>
                </div>
                <div class="compact-card-content">
                    <p class="compact-card-description">عرض واعتماد بطاقات التشغيل المعتمدة</p>
                    <div class="compact-card-action">
                        <a href="print_preview.html" class="compact-btn">
                            <i class="fas fa-id-card"></i>
                            عرض البطاقات
                        </a>
                    </div>
                </div>
            </div> -->

            <!-- <div class="compact-action-card">
                <div class="compact-card-header">
                    <div class="compact-card-icon">
                        <i class="fas fa-file-medical"></i>
                    </div>
                    <div class="compact-card-title">
                        <h6>الشهادات الصحية</h6>
                    </div>
                </div>
                <div class="compact-card-content">
                    <p class="compact-card-description">عرض واعتماد الشهادات الصحية المعتمدة</p>
                    <div class="compact-card-action">
                        <a href="print_preview.html" class="compact-btn">
                            <i class="fas fa-certificate"></i>
                            عرض الشهادات
                        </a>
                    </div>
                </div>
            </div> -->
<!-- 
            <div class="compact-action-card">
                <div class="compact-card-header">
                    <div class="compact-card-icon">
                        <i class="fas fa-cog"></i>
                    </div>
                    <div class="compact-card-title">
                        <h6>إعدادات النظام</h6>
                    </div>
                </div>
                <div class="compact-card-content">
                    <p class="compact-card-description">تكوين وإدارة إعدادات النظام العامة والأمان</p>
                    <div class="compact-card-action">
                        <a href="settingsUser" class="compact-btn">
                            <i class="fas fa-tools"></i>
                            الإعدادات
                        </a>
                    </div>
                </div>
            </div> -->

         
        </div>

        <!-- Features Section -->
        <div class="row mb-4">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h4 class="mb-0">مميزات النظام</h4>
                    </div>
                </div>
            </div>
        </div>

        <div class="features-grid">
            <div class="feature-card">
                <div class="icon">
                    <i class="fas fa-file-pdf"></i>
                </div>
                <h6>طباعة PDF</h6>
                <p>إنشاء وطباعة ملفات PDF عالية الجودة مع تنسيق احترافي.</p>
            </div>

            <div class="feature-card">
                <div class="icon">
                    <i class="fas fa-shield-alt"></i>
                </div>
                <h6>التحقق من البيانات</h6>
                <p>تحقق شامل من صحة البيانات المدخلة مع رسائل تنبيه واضحة.</p>
            </div>

            <div class="feature-card">
                <div class="icon">
                    <i class="fas fa-calendar-alt"></i>
                </div>
                <h6>تحويل التواريخ التلقائي</h6>
                <p>تحويل تلقائي بين التقويم الميلادي والهجري مع حساب المدة الدقيقة.</p>
            </div>

            <div class="feature-card">
                <div class="icon">
                    <i class="fas fa-save"></i>
                </div>
                <h6>حفظ آمن</h6>
                <p>حفظ آمن للبيانات مع إمكانية الاسترجاع والنسخ الاحتياطي.</p>
            </div>

            <div class="feature-card">
                <div class="icon">
                    <i class="fas fa-mobile-alt"></i>
                </div>
                <h6>متجاوب مع الأجهزة</h6>
                <p>تصميم متجاوب يعمل بكفاءة على جميع الأجهزة والشاشات.</p>
            </div>

            <div class="feature-card">
                <div class="icon">
                    <i class="fas fa-search"></i>
                </div>
                <h6>البحث المتقدم</h6>
                <p>بحث سريع بالاسم أو رقم الإجازة مع عرض النتائج الفوري.</p>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script>
    const monthlyLeaves = document.querySelector('#monthlyLeaves')
    const activeUsers = document.querySelector('#activeUsers')
    const todayLeaves = document.querySelector('#todayLeaves')
    fetch('/api/leave-count/current-month')
  .then(response => response.json())
  .then(data => {
    monthlyLeaves.textContent = data.total;
  })
  .catch(error => console.error('خطأ في جلب عدد الإجازات:', error));
    fetch('/api/user-count')
  .then(response => response.json())
  .then(data => {
    activeUsers.textContent = data.count;
  })
  .catch(error => console.error('خطأ في جلب عدد الإجازات:', error));
    fetch('/api/today')
  .then(response => response.json())
  .then(data => {
    console.log(data);
    todayLeaves.textContent = data.count;
  })
  .catch(error => console.error('خطأ في جلب عدد الإجازات:', error));

     const user2 = document.querySelector('.greeting');
  const username2 = localStorage.getItem('username');

  user2.textContent = username2;
    document.addEventListener('DOMContentLoaded', function() {
        const navToggle = document.getElementById('navToggle');
        const floatingNav = document.getElementById('floatingNav');
        
        // Navigation toggle functionality
        navToggle.addEventListener('click', function() {
            floatingNav.classList.toggle('show');
            
            const icon = navToggle.querySelector('i');
            if (floatingNav.classList.contains('show')) {
                icon.className = 'fas fa-times';
            } else {
                icon.className = 'fas fa-bars';
            }
        });

        // Close floating nav when clicking outside
        document.addEventListener('click', function(event) {
            if (!navToggle.contains(event.target) && !floatingNav.contains(event.target)) {
                floatingNav.classList.remove('show');
                const icon = navToggle.querySelector('i');
                icon.className = 'fas fa-bars';
            }
        });

        // Real-time clock and date updates
        function updateDateTime() {
            const now = new Date();
            
            // Update time
            const timeString = now.toLocaleTimeString('ar-SA', {
                hour12: false,
                hour: '2-digit',
                minute: '2-digit',
                second: '2-digit'
            });
            document.getElementById('currentTime').textContent = timeString;
            
            // Update Gregorian date
            const gregorianDate = new Date().toLocaleDateString('en-SA', {
    day: 'numeric',
    month: 'long',
    year: 'numeric'
});
            document.getElementById('gregorianDate').textContent = gregorianDate;
            
            // Update Hijri date (approximation)
            const hijriDate = now.toLocaleDateString('ar-SA-u-ca-islamic', {
                day: 'numeric',
                month: 'long',
                year: 'numeric'
            });
            document.getElementById('hijriDate').textContent = hijriDate;
            
            // Update greeting based on time
            const hour = now.getHours();
            let greeting;
            if (hour < 6) {
                greeting = "نتمنى لك ليلة هادئة";
            } else if (hour < 12) {
                greeting = "نتمنى لك صباحاً مشرقاً";
            } else if (hour < 17) {
                greeting = "نتمنى لك ظهيرة مباركة";
            } else if (hour < 20) {
                greeting = "نتمنى لك مساءً سعيداً";
            } else {
                greeting = "نتمنى لك ليلة هادئة";
            }
            document.getElementById('timeGreeting').textContent = greeting;
        }





        // Initialize and set intervals
        updateDateTime();
        
        // Update time every second
        setInterval(updateDateTime, 1000);

        // Add smooth animations on scroll
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        // Observe all cards for animation
        document.querySelectorAll('.stat-card, .action-card, .feature-card').forEach(card => {
            card.style.opacity = '0';
            card.style.transform = 'translateY(20px)';
            card.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
            observer.observe(card);
        });
    });
</script>
</body>
</html>


<style>
    /* تحسينات خاصة لبطاقات الإجراءات السريعة المدمجة */
    .compact-action-cards {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.2rem;
        margin-bottom: 2rem;
    }

    .compact-action-card {
        background: var(--bg-primary);
        border-radius: 16px;
        padding: 1.5rem;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        border: 1px solid var(--border-light);
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        min-height: 200px;
    }

    .compact-action-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 3px;
        background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
        transform: scaleX(0);
        transition: transform 0.3s ease;
    }

    .compact-action-card:hover::before {
        transform: scaleX(1);
    }

    .compact-action-card:hover {
        transform: translateY(-6px);
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        border-color: var(--primary-light);
    }

    .compact-card-header {
        display: flex;
        align-items: center;
        margin-bottom: 1rem;
    }

    .compact-card-icon {
        width: 48px;
        height: 48px;
        border-radius: 12px;
        background: linear-gradient(135deg, var(--primary-lightest) 0%, var(--secondary-lightest) 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        margin-left: 1rem;
        transition: all 0.3s ease;
    }

    .compact-card-icon i {
        font-size: 1.5rem;
        color: var(--primary-color);
        transition: all 0.3s ease;
    }

    .compact-action-card:hover .compact-card-icon {
        background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
        transform: scale(1.1);
    }

    .compact-action-card:hover .compact-card-icon i {
        color: white;
        transform: scale(1.1);
    }

    .compact-card-title {
        flex: 1;
    }

    .compact-card-title h6 {
        color: var(--text-primary);
        font-weight: 700;
        margin: 0;
        font-size: 1rem;
        line-height: 1.4;
        font-family: 'Cairo', sans-serif;
    }

    .compact-card-content {
        flex: 1;
        display: flex;
        flex-direction: column;
    }

    .compact-card-description {
        color: var(--text-muted);
        font-size: 0.875rem;
        line-height: 1.5;
        margin-bottom: 1.25rem;
        font-family: 'Cairo', sans-serif;
        flex: 1;
    }

    .compact-card-action {
        margin-top: auto;
    }

    .compact-btn {
        background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
        color: white;
        border: none;
        padding: 0.75rem 1.5rem;
        border-radius: 10px;
        font-weight: 600;
        font-size: 0.875rem;
        transition: all 0.3s ease;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        font-family: 'Cairo', sans-serif;
        position: relative;
        overflow: hidden;
        width: 100%;
    }

    .compact-btn::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
        transition: left 0.5s;
    }

    .compact-btn:hover::before {
        left: 100%;
    }

    .compact-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 15px -3px rgba(0, 102, 204, 0.3);
        color: white;
        text-decoration: none;
    }

    .compact-btn:active {
        transform: translateY(0);
    }

    .compact-btn i {
        margin-right: 0.5rem;
        font-size: 0.875rem;
    }

    /* تحسين عنوان القسم */
    .section-header {
        background: var(--bg-primary);
        border-radius: 16px;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        border: 1px solid var(--border-light);
        margin-bottom: 1.5rem;
        overflow: hidden;
    }

    .section-header .card-header {
        background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
        color: white;
        padding: 1.25rem 1.5rem;
        border: none;
        font-weight: 700;
        font-size: 1.125rem;
        display: flex;
        align-items: center;
    }

    .section-header .card-header i {
        margin-left: 0.75rem;
        font-size: 1.25rem;
    }

    /* تحسينات للشاشات الصغيرة */
    @media (max-width: 768px) {
        .compact-action-cards {
            grid-template-columns: 1fr;
            gap: 1rem;
        }
        
        .compact-action-card {
            padding: 1.25rem;
            min-height: 180px;
        }
        
        .compact-card-icon {
            width: 44px;
            height: 44px;
        }
        
        .compact-card-icon i {
            font-size: 1.25rem;
        }
    }

    @media (max-width: 480px) {
        .compact-action-cards {
            gap: 0.75rem;
        }
        
        .compact-action-card {
            padding: 1rem;
            min-height: 160px;
        }
        
        .compact-card-header {
            margin-bottom: 0.75rem;
        }
        
        .compact-card-description {
            font-size: 0.8rem;
            margin-bottom: 1rem;
        }
    }

    /* تأثيرات إضافية للتفاعل */
    .compact-action-card {
        animation: fadeInUp 0.6s ease-out;
        animation-fill-mode: both;
    }

    .compact-action-card:nth-child(1) { animation-delay: 0.1s; }
    .compact-action-card:nth-child(2) { animation-delay: 0.2s; }
    .compact-action-card:nth-child(3) { animation-delay: 0.3s; }
    .compact-action-card:nth-child(4) { animation-delay: 0.4s; }
    .compact-action-card:nth-child(5) { animation-delay: 0.5s; }
    .compact-action-card:nth-child(6) { animation-delay: 0.6s; }
    .compact-action-card:nth-child(7) { animation-delay: 0.7s; }
    .compact-action-card:nth-child(8) { animation-delay: 0.8s; }

    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>